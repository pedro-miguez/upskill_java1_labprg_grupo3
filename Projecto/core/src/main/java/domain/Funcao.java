package domain;

/**
 * Current class is an Enum class that gives us the options available for the roles that a member of an organization
 * (Organizacao) can have.
 *
 */
public enum Funcao {
    /**
     * Manager role (Gestor).
     */
    GESTOR,
    /**
     * Employee role (Colaborador).
     */
    COLABORADOR
}
